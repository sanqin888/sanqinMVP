{
  "schemaVersion": 1,
  "baselineCommit": "443b1a5c",
  "updatedAt": "2026-09-02",
  "requiredFields": [
    "compat_id",
    "old_owner",
    "new_owner",
    "dual_write_or_read",
    "parity_metric",
    "rollback",
    "exit_criteria",
    "remove_pr",
    "deadline"
  ],
  "active": [
    {
      "compat_id": "brand-store.default-store-identity.v1",
      "status": "active",
      "old_owner": "Implicit literal default store identity in Uber channel tables, BusinessHour/Holiday persistence defaults, and single-store helpers",
      "new_owner": "Explicit Store.storeStableId at public boundaries with owner-local storeDbId conversion at persistence boundaries",
      "dual_write_or_read": [
        "Staff Brand/Store schedule reads and writes plus StoreStatus now pass storeStableId through STORE_SCHEDULE_READER/WRITER and resolve storeDbId only inside the Store Prisma adapter",
        "BusinessHour and Holiday no longer carry hard-coded store DB UUID defaults in Prisma; the store-scope migration keeps the physical storeId column, exposes it as storeDbId in Prisma, and changes BusinessHour uniqueness to storeDbId + weekday",
        "Authenticated POS Orders and Daily Summary browser timezone context now comes from /pos/store-context: PosDeviceGuard supplies the authenticated device storeStableId and the POS adapter reads that exact Store snapshot through BRAND_STORE_CONFIG_READER instead of falling back to /staff/store/config",
        "Authenticated POS order creation, recent/board reads, per-order management/printing/status actions, scheduled-order timing/manual preparation, and managed refund entry points carry the PosDeviceGuard storeStableId through store-scoped Orders boundaries. The first NULL-store contraction batch is now staged: store-scoped Orders and scheduling queries match only canonical storeId, scheduled preparation no longer accepts storeId IS NULL, and accepted/reprint/amendment print dispatch fails closed instead of guessing the configured store when order.storeId is missing; production Order.storeId IS NULL was verified as zero before this contraction",
        "Authenticated POS store-status reads, pause/resume writes, and timed-pause compare-and-set reconciliation now carry PosDeviceGuard storeStableId through the Brand/Store reader/writer boundary. The configured-store connectivity watchdog resolves its deployment store once, scopes ACTIVE PosDevice heartbeats to that Store relation, and passes the same explicit storeStableId through StoreStatus and pause reconciliation instead of relying on owner-internal fallback selection. StoreConfig pause/resume persistence is canonical-only because the former singleton BusinessConfig compatibility persistence has been fully contracted",
        "Canonical Admin Web Store config, hours, and holidays calls now require storeStableId and use only /staff/stores/:storeStableId/*; the selector establishes ?store= from the Store directory before Store settings load, while legacy /staff/store/* routes remain isolated as an annotated stale-bundle compatibility adapter",
        "Eight Uber channel/ops persistence models still declare storeId @default(\"default\") even though production rows using the literal default identity were verified as zero across all eight tables on 2026-09-02; remaining Uber readers/writers still need an explicit-store contraction audit before those schema defaults are removed",
        "Other existing rows and callers may still omit an explicit store identity"
      ],
      "parity_metric": "Readiness check on 2026-09-02: Order rows with storeId IS NULL = 0; rows with storeId='default' = 0 across UberItemChannelConfig, UberCategoryConfig, UberModifierGroupConfig, UberOptionItemConfig, UberOptionChildGroupBinding, UberMenuPublishVersion, UberReconciliationReport, and UberOpsTicket; no real /api/v1/staff/store/* request appeared after deployment while canonical /api/v1/staff/stores/4750_Yonge_Street/* reads/writes returned 200. Remaining exit work is code/schema contraction of the now-unused fallbacks/defaults plus direct regression verification, not an organic-traffic waiting period",
      "rollback": "Retain the old nullable/default columns during expand-backfill-cutover; do not reinterpret historical IDs",
      "exit_criteria": "All rows are backfilled, callers pass storeStableId, schema defaults and fallback helpers are removed",
      "remove_pr": "Phase 2 store identity contraction PR",
      "deadline": "Before Phase 2 Brand/Store exit",
      "evidence": [
        "apps/api/prisma/schema.prisma BusinessHour/Holiday and Uber*Config models",
        "apps/api/src/store/store-schedule.contract.ts",
        "apps/api/src/store/brand-store-config.reader.ts",
        "apps/api/src/store/store-status.service.ts",
        "apps/api/src/pos/pos-summary.controller.ts",
        "apps/api/src/pos/pos-orders.controller.ts",
        "apps/api/src/pos/pos-orders.service.ts",
        "apps/api/src/pos/pos-store-status.controller.ts",
        "apps/api/src/pos/pos-store-status.service.ts",
        "apps/api/src/pos/pos-connectivity-watchdog.service.ts",
        "apps/api/src/store/store-status.controller.ts",
        "apps/api/src/store/store-status.service.ts",
        "apps/api/src/orders/orders.controller.ts",
        "apps/api/src/orders/orders.service.ts",
        "apps/api/src/pos/legacy-pos-orders.controller.ts",
        "apps/api/src/orders/order-scheduling-query.service.ts",
        "apps/api/src/orders/order-preparation.service.ts",
        "apps/api/src/orders/processors/fulfillment.processor.ts",
        "apps/api/src/orders/processors/fulfillment.processor.spec.ts",
        "apps/api/src/admin/business/staff-brand-store.controller.ts",
        "apps/web/src/lib/api/pos-session.ts",
        "apps/web/src/lib/api/brand-store.ts",
        "apps/web/src/features/admin/brand-store/AdminStoreContextSelector.tsx",
        "apps/web/src/features/admin/brand-store/StoreSettingsPageClient.tsx",
        "SanQ full-site modularization audit section 3.3, 6.2 and 6.8"
      ]
    },
    {
      "compat_id": "orders.pos-transport-routes.v1",
      "status": "active",
      "old_owner": "POS-specific HTTP routes mounted from OrdersController and ScheduledOrdersController under /orders/*, with Orders importing POS device transport authentication",
      "new_owner": "Store Operations/POS-owned canonical /pos/orders/* transport calling the Orders-owned POS_ORDER_OPERATIONS public application boundary",
      "dual_write_or_read": [
        "Canonical POS recent/board/status/amendment/advance routes use /pos/orders/* and call POS_ORDER_OPERATIONS instead of importing OrdersService, Orders DTOs, status internals, or scheduling implementation",
        "Canonical scheduled queue, fulfillment timing, and manual preparation routes are available under /pos/orders/*; the current Web POS scheduled rail polls /pos/orders/scheduled",
        "Legacy /orders/recent, /orders/board, /orders/:orderStableId/status, /orders/:orderStableId/amendments, /orders/:orderStableId/advance, /orders/scheduled, /orders/:orderStableId/fulfillment-timing, and /orders/:orderStableId/preparation/start remain mounted by a POS-owned compatibility controller with their existing guards, response shapes, status codes, and store scoping",
        "Healthy canonical /pos/orders/scheduled polling is quiet in RequestIdInterceptor while legacy /orders/scheduled traffic is intentionally observable so stale clients can be measured before contraction",
        "The shared /orders HTTP prefix is composed at the API root so the POS-owned compatibility controller is registered before generic Orders routes without restoring OrdersModule -> PosDeviceModule",
        "Orders fulfillment keeps the existing print payload and durable retry behavior but requests PosPrintJob dispatch through an Orders-owned event contract; the POS-owned listener is the only side of this boundary that invokes PosGateway, so no printer payload or Socket protocol changes are introduced"
      ],
      "parity_metric": "Repository consumers of the legacy POS /orders/* routes must be zero, and RequestIdInterceptor traffic to every legacy route family must remain zero through an agreed stale-client observation window; canonical /pos/orders/* behavior remains covered by POS controller characterization tests",
      "rollback": "Keep the POS-owned legacy compatibility controller mounted while canonical clients migrate; if a stale client is discovered it continues to use the same Orders public operation boundary without restoring Orders-to-POS imports",
      "exit_criteria": "All known Web/POS consumers use /pos/orders/*, legacy route traffic is zero for the agreed observation window, no operational client requires the old URLs, and the user explicitly authorizes contract contraction",
      "remove_pr": "Follow-up legacy POS /orders/* HTTP route contraction after production observation and explicit authorization",
      "deadline": "Before Commerce/Orders and Store Operations/POS boundary modularization exit",
      "evidence": [
        "apps/api/src/orders/public-api.ts",
        "apps/api/src/orders/pos-order-operations.contract.ts",
        "apps/api/src/orders/pos-order-operations.service.ts",
        "apps/api/src/orders/pos-print-dispatch.contract.ts",
        "apps/api/src/orders-http-composition.module.ts",
        "apps/api/src/auth/public-api.ts",
        "apps/api/src/pos/pos-orders.controller.ts",
        "apps/api/src/pos/legacy-pos-orders.controller.ts",
        "apps/api/src/pos/pos-print-dispatch.listener.ts",
        "apps/api/src/pos/pos-orders-transport-boundary.architecture.spec.ts",
        "apps/api/src/common/request-id.interceptor.ts",
        "apps/web/src/components/store/PosDeviceFrame.tsx"
      ]
    },
    {
      "compat_id": "pos-device.admin-db-id.v1",
      "status": "active",
      "old_owner": "Admin POS-device browser contract exposed PosDevice.id and PosDevice.storeId database UUIDs and accepted those UUIDs for device actions/store assignment",
      "new_owner": "Store Operations/POS PosDevice management boundary using deviceStableId and storeStableId for all canonical Admin/Web operations",
      "dual_write_or_read": [
        "Canonical Admin POS-device list requests include storeStableId and return only deviceStableId/storeStableId business identities",
        "Canonical create, reset, status, and delete operations use storeStableId/deviceStableId through POS_DEVICE_MANAGEMENT",
        "The no-query legacy list temporarily keeps id/storeId field names but maps them to deviceStableId/storeStableId so old bundles remain operational without emitting database UUIDs",
        "A stale browser tab may still submit a previously loaded device DB UUID or legacy create storeId UUID; POS_DEVICE_ADMIN_COMPATIBILITY resolves those inbound UUIDs to stable identities before canonical management executes"
      ],
      "parity_metric": "New Admin Web requests containing storeStableId must use zero database UUID fields; API logs with event=pos_device_admin_compatibility_used (legacy_unscoped_list, legacy_store_db_id, implicit_default_store, legacy_device_db_id) must reach zero after deployment/browser refresh observation",
      "rollback": "Keep the inbound UUID resolver and legacy no-query response aliases while old Admin bundles may still be open; canonical stable-ID routes remain the preferred path and no database UUID is reintroduced into new Web state",
      "exit_criteria": "Admin Web has used only storeStableId/deviceStableId for one observation window, legacy no-query list traffic and UUID resolver usage are zero, then remove storeId DTO input, legacy response aliases, UUID resolvers, annotations, and this active compatibility entry",
      "remove_pr": "Follow-up POS-device Admin DB-ID contraction after production observation",
      "deadline": "Before Store Operations/POS identity modularization exit",
      "evidence": [
        "apps/api/src/pos/public-api.ts",
        "apps/api/src/pos/pos-device-management.contract.ts",
        "apps/api/src/pos/pos-device.service.ts",
        "apps/api/src/admin/pos-devices/admin-pos-devices.controller.ts",
        "apps/web/src/lib/api/pos-devices.ts",
        "apps/web/src/app/[locale]/(site)/admin/(protected)/pos-devices/page.tsx"
      ]
    },
    {
      "compat_id": "web.api-envelope-direct-payload.v1",
      "status": "active",
      "old_owner": "Checkout direct API payload readers and page-local response unwrap helpers",
      "new_owner": "One Web API client using the global code/message/details response envelope",
      "dual_write_or_read": [
        "Canonical apiFetch and serverApiFetch require the global code/message/details envelope",
        "POS session/login claim, login, auth/me, public store-status and connectivity heartbeat calls use the centralized POS session API adapter; their five browser direct-fetch allowances are removed",
        "Legacy browser direct-fetch debt remains only in Checkout (6 calls), which still owns page-local envelope/direct-payload compatibility"
      ],
      "parity_metric": "Architecture scanner browser direct-fetch debt is reduced from 11 to 6 and must reach zero; POS session/login direct fetches must remain zero, and page-local Checkout envelope/direct-payload readers must be removed",
      "rollback": "Keep only the remaining Checkout legacy calls isolated by architecture allowances until its dedicated risk-scoped migration; POS session/login must stay on the canonical client and must not regain direct-fetch allowances",
      "exit_criteria": "All regular Web JSON API calls use the canonical client, remaining legacy browser direct fetches are zero, and duplicate/page-local ApiEnvelope readers are removed",
      "remove_pr": "POS session/login browser transport migration followed by the dedicated Checkout browser transport migration",
      "deadline": "Before Phase 1 low-risk cleanup exit",
      "evidence": [
        "SanQ full-site modularization audit section 3.4",
        "apps/web/src/lib/api/pos-session.ts",
        "apps/web/src/app/[locale]/(device)/store/pos/PosSessionKeepAlive.tsx",
        "apps/web/src/app/[locale]/(device)/store/pos/login/page.tsx",
        "apps/api/src/pos/pos-devices.controller.ts",
        "tools/architecture/context-baseline.json"
      ]
    },
    {
      "compat_id": "payments.pos-card-legacy.v1",
      "status": "frozen",
      "old_owner": "POS CARD /pos/orders direct paid-order creation",
      "new_owner": "Unified Payment Core plus Clover Terminal followed by idempotent order finalization",
      "dual_write_or_read": [
        "Feature flag false routes to legacy CARD",
        "Feature flag true is reserved for the new terminal flow; the same operation must never call both"
      ],
      "parity_metric": "Terminal payment/provider truth, amount, surcharge, final order binding and refund outcome parity across one complete settlement cycle",
      "rollback": "Set the rollout flag false to route new POS operations back to legacy CARD without rewriting payment history",
      "exit_criteria": "Clover account blocker resolved, real-device acceptance complete, one settlement cycle reconciled, legacy invocation metric is zero",
      "remove_pr": "Payments/Clover Phase G legacy POS contraction PR",
      "deadline": "Before Phase 5B Payments/Clover exit; clock resumes when Clover sandbox identity blocker is resolved",
      "freeze_reason": "Clover Developer sandbox test merchant does not recognize the developer identity; awaiting Clover support",
      "evidence": [
        "docs/payments/clover-pos-integration-charter.md sections 9 and 11"
      ]
    },
    {
      "compat_id": "payments.web-checkout-v1.v1",
      "status": "frozen",
      "old_owner": "CheckoutIntent, CloverPayController/CloverService and Clover Ecommerce /v1 charge path",
      "new_owner": "Channel-neutral Unified Payment Core using canonical Clover Platform v3 truth",
      "dual_write_or_read": [
        "Legacy Web Ecommerce remains the production path until Phase G",
        "Unified core exists but Web traffic must not be switched early"
      ],
      "parity_metric": "Payment ID, amount, surcharge, refund, settlement and old-path invocation metrics across one complete settlement cycle",
      "rollback": "Keep legacy Web routing available until the new Web consumer is accepted; traffic rollback must not rewrite historical facts",
      "exit_criteria": "Uber/Clover external blockers no longer constrain validation, Web cutover is accepted, one settlement cycle matches, old calls are zero",
      "remove_pr": "Payments/Clover Phase G legacy Web contraction PR",
      "deadline": "Before Phase 5B Payments/Clover exit; clock resumes when external verification blockers are resolved",
      "freeze_reason": "Clover sandbox identity verification is blocked; current Web production behavior is explicitly frozen",
      "evidence": [
        "docs/payments/clover-pos-integration-charter.md sections 3.8, 9.1 and 12.5"
      ]
    }
  ],
  "closed": [
    {
      "compat_id": "brand-store.business-config.v1",
      "status": "closed",
      "old_owner": "BusinessConfig single-store compatibility model plus its one-way BusinessConfig-to-canonical trigger",
      "new_owner": "BrandConfig plus per-store StoreConfig behind the Brand/Store reader/writer boundary; Benefits loyalty policy is owned separately by LoyaltyProgramPolicy",
      "dual_write_or_read": "Removed. Application runtime uses only canonical BrandConfig/StoreConfig persistence; the BusinessConfig Prisma model, physical table, BusinessConfig_sync_canonical_config trigger, and syncBusinessConfigToCanonicalConfig() function are all gone.",
      "parity_metric": "Before destructive contraction, direct production verification proved the application mirror was inert and the final 29 overlapping Brand/Store fields were zero-diff. Migration 20260902044000_contract_brand_store_business_config rechecked row counts, all 29 fields, trigger/function binding, and database dependencies under locks before dropping legacy persistence. Post-deployment verification on 2026-09-02 confirmed the migration applied exactly once, BusinessConfig/trigger/function are absent, BrandConfig/Store/StoreConfig canonical rows remain, Admin PATCH /api/v1/staff/brand/config returned 200 and persisted wechatAlipayExchangeRate=5.2, POS pause/resume both returned 200 with final isTemporarilyClosed=false, both Uber store-status syncs succeeded, and API/worker error scans were clean.",
      "rollback": "Forward-fix only after destructive contraction. Do not recreate BusinessConfig, its sync trigger/function, or an application mirror; BrandConfig/StoreConfig are the canonical persistence owners.",
      "exit_criteria": "Completed: runtime BusinessConfig delegates are zero, the Prisma model and physical legacy persistence are removed, canonical rows survived migration, and direct Admin/POS/Uber production behavior remains healthy after deletion.",
      "remove_pr": "PR #2101 / merge 277a5276; migration 20260902044000_contract_brand_store_business_config",
      "deadline": "Completed 2026-09-02",
      "evidence": [
        "apps/api/prisma/schema.prisma",
        "apps/api/prisma/migrations/20260902044000_contract_brand_store_business_config/migration.sql",
        "apps/api/src/store/brand-store-config.reader.ts",
        "apps/api/src/store/brand-store-config.writer.spec.ts",
        "apps/api/src/pos/pos-store-status.service.ts",
        "apps/api/src/admin/business/admin-business.service.ts",
        "tools/architecture/context-baseline.json",
        "tools/architecture/scan-architecture.mjs",
        "PR #2099 application mirror contraction",
        "PR #2101 destructive persistence contraction",
        "production verification 2026-09-02 01:08-01:12 America/Toronto"
      ]
    },
    {
      "compat_id": "benefits.business-config-loyalty-policy.v1",
      "status": "closed",
      "old_owner": "BusinessConfig/BrandConfig duplicated Loyalty policy columns plus the BusinessConfig-to-canonical trigger and transitional Benefits shadow/triple-write persistence",
      "new_owner": "Benefits-owned LoyaltyPolicy public boundary backed solely by LoyaltyProgramPolicy(id=1)",
      "dual_write_or_read": "Removed. Benefits runtime/settings/transaction reads and writes use only LoyaltyProgramPolicy; BrandConfig/BusinessConfig no longer contain Loyalty policy columns and the surviving Brand/Store trigger contains no Loyalty propagation",
      "parity_metric": "Pre-contraction production parity on 2026-09-01 was zero-diff across all ten policy fields. Post-deployment direct verification proved Admin Loyalty PATCH 200 and canonical restore, POS /pos/loyalty-policy 200, Web pure-points order with externalCents=0 plus full refund and exact REDEEM_ON_ORDER/REFUND_RETURN_REDEEM ledger reversal, public membership rules 200, unrelated Store hours change/revert without LoyaltyProgramPolicy.updatedAt movement, zero old Loyalty columns, and a Loyalty-free trigger function",
      "rollback": "Forward-fix only after the destructive Phase D migration. Do not recreate dropped BrandConfig/BusinessConfig Loyalty columns, invent defaults, reinterpret historical redemption facts, or rewrite Loyalty ledger history",
      "exit_criteria": "Completed: old Admin Business Loyalty request/response ownership is contracted, all runtime readers/writers use LoyaltyProgramPolicy, the trigger contains no Loyalty fields, duplicate columns are dropped, direct production verification is clean, and source compatibility annotations are absent",
      "remove_pr": "Phase D contraction PR #2095 plus refactor/loyalty-phase-d-closeout",
      "deadline": "Completed 2026-09-01",
      "evidence": [
        "apps/api/src/loyalty/public-api.ts",
        "apps/api/src/loyalty/loyalty-policy.contract.ts",
        "apps/api/src/loyalty/loyalty-policy-prisma.writer.ts",
        "apps/api/src/admin/benefits/admin-loyalty-policy.controller.ts",
        "apps/api/src/admin/business/admin-business.controller.ts",
        "apps/api/src/admin/business/admin-business.service.ts",
        "apps/api/src/orders/orders-loyalty-redemption.ts",
        "apps/api/src/orders/orders-loyalty-redemption.characterization.spec.ts",
        "apps/api/prisma/schema.prisma",
        "apps/api/prisma/migrations/20260901235900_contract_loyalty_policy_storage/migration.sql",
        "docs/architecture/benefits-loyalty-policy-contraction-plan.md",
        "tools/architecture/context-baseline.json",
        "tools/architecture/scan-architecture.mjs",
        "production verification 2026-09-01 22:57-23:04 America/Toronto"
      ]
    },
    {
      "compat_id": "orders.order-item-components.v1",
      "status": "closed",
      "old_owner": "OrderItem.componentsJson historical fixed-combo reconstruction path",
      "new_owner": "Order-owned immutable item component snapshot",
      "dual_write_or_read": "Removed after production dry-run and contraction",
      "parity_metric": "Production dry-run returned zero actionable rows",
      "rollback": "Git history and the completed audit record; no live fallback retained",
      "exit_criteria": "Backfill planner/CLI and legacy read reconstruction removed",
      "remove_pr": "#2004",
      "deadline": "Completed 2026-08-30",
      "evidence": [
        "docs/orders/order-item-components-backfill.md",
        "commit b8413cd7"
      ]
    }
  ],
  "reviewCandidates": [
    {
      "candidate": "EventEmitter aliases alongside durable outbox events",
      "decisionNeeded": "Measure consumers and duplicate side effects before assigning a compat_id"
    }
  ]
}
