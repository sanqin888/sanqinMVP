{
  "schemaVersion": 1,
  "baselineCommit": "e8a0fec8",
  "updatedAt": "2026-08-31",
  "requiredFields": [
    "compat_id",
    "old_owner",
    "new_owner",
    "dual_write_or_read",
    "parity_metric",
    "rollback",
    "exit_criteria",
    "remove_pr",
    "deadline"
  ],
  "active": [
    {
      "compat_id": "brand-store.business-config.v1",
      "status": "active",
      "old_owner": "BusinessConfig single-store compatibility model and the remaining POS pause/resume compatibility writer",
      "new_owner": "BrandConfig plus per-store StoreConfig behind the Brand/Store reader/writer boundary; Benefits loyalty policy is tracked separately",
      "dual_write_or_read": [
        "Orders delivery pricing, sales tax, store coordinates, Uber Direct enablement, and store-local timezone reads use BRAND_STORE_CONFIG_READER; Orders no longer reads or creates BusinessConfig",
        "POS StoreStatus and Connectivity reads now use BRAND_STORE_CONFIG_READER, including the watchdog recovery race check; POS pause/resume persistence still writes BusinessConfig as an explicit compatibility write so the existing one-way BusinessConfig-to-StoreConfig trigger keeps legacy readers synchronized",
        "Admin Business reads use BRAND_STORE_CONFIG_READER and Brand/Store writes use BRAND_STORE_CONFIG_WRITER; Admin no longer writes BusinessConfig, BrandConfig, or StoreConfig through Prisma directly. The Brand/Store owner writer persists canonical rows first and refreshes the complete overlapping BusinessConfig compatibility copy in the same transaction so later POS compatibility writes cannot replay stale Brand/Store values",
        "Retained Loyalty fields on the legacy Admin Business rollback surface are delegated to LOYALTY_POLICY_WRITER rather than being mixed into Brand/Store persistence",
        "Uber menu schedule/tax and store-status source reads use an Uber application-owned store-config query port wired only in ubereats.module.ts to BRAND_STORE_CONFIG_READER; Uber production code no longer reads or creates BusinessConfig, while existing response source labels remain unchanged for compatibility",
        "The BusinessConfig compatibility trigger remains active while the POS compatibility writer is migrated; the Brand/Store owner writer is the only Brand/Store boundary allowed to maintain the compatibility copy"
      ],
      "parity_metric": "Per-field BrandConfig/StoreConfig versus BusinessConfig difference report must remain zero for one complete business cycle; Admin direct BusinessConfig/BrandConfig/StoreConfig writes must remain zero",
      "rollback": "Brand/Store production reads and Admin writes are canonical; keep the owner-maintained BusinessConfig compatibility copy and one-way trigger until the remaining POS writer is migrated and write parity is proven",
      "exit_criteria": "All Brand/Store fields have one owner, POS and all other consumers no longer read or write BusinessConfig directly, then the Brand/Store compatibility copy and trigger can be contracted",
      "remove_pr": "Phase 2 Brand/Store contraction PR (to be created after expand/backfill/cutover)",
      "deadline": "Before Phase 2 Brand/Store exit",
      "evidence": [
        "apps/api/prisma/schema.prisma",
        "apps/api/src/store/brand-store-config.reader.ts",
        "apps/api/src/admin/business/admin-business.service.ts",
        "SanQ full-site modularization audit section 3.3 and 6.2"
      ]
    },
    {
      "compat_id": "benefits.business-config-loyalty-policy.v1",
      "status": "active",
      "old_owner": "BusinessConfig still retains a synchronized Loyalty compatibility copy while its one-way trigger can replay those columns into BrandConfig; the legacy Admin Business Loyalty request/response contract has now been contracted and no longer owns policy reads or writes",
      "new_owner": "Benefits-owned LoyaltyPolicy reader/writer boundary plus the expanded LoyaltyProgramPolicy persistence table; BrandConfig remains transitional runtime canonical storage until the Phase B triple-write/shadow-read release and later read cutover",
      "dual_write_or_read": [
        "The Benefits writer validates policy patches, reads the existing BrandConfig policy as canonical state without runtime defaults, and updates BrandConfig plus the BusinessConfig compatibility copy in one transaction so later unrelated BusinessConfig-triggered syncs cannot restore stale Loyalty values",
        "Phase A adds LoyaltyProgramPolicy(id=1) with no business-field defaults and backfills it from BrandConfig(id=1); the migration atomically rejects missing singleton rows and reports exact per-field parity differences against BrandConfig and the active BusinessConfig compatibility copy. Runtime code does not read or write the new table yet, so it is not a canonical source until Phase B begins maintaining it",
        "Admin Members reads and writes use /admin/benefits/loyalty-policy; Admin Settings no longer owns or resubmits Loyalty fields; the legacy Admin Business request DTOs and response no longer expose Loyalty fields, AdminBusinessService has no Benefits policy reader/writer dependency, and stale PATCH/PUT callers receive HTTP 400 directing them to the Benefits endpoint; repository-wide Web old Admin Business route plus Loyalty-field usage is gated at zero",
        "POS payment reads the runtime Benefits policy through /pos/loyalty-policy with Session, Role, and PosDevice guards instead of reading Admin Business config",
        "Orders quote/create redemption conversion reads redeemDollarPerPoint through the Benefits runtime policy reader; delivery, tax, timezone, and other remaining BusinessConfig reads are tracked separately as Brand/Store debt",
        "All LoyaltyService transaction and non-transaction readers plus Admin member tier progress still read Benefits policy from BrandConfig during Phase A",
        "The implementation-ready persistence plan keeps trigger splitting separate from the initial Benefits table expand so rollback never depends on a prematurely removed BusinessConfig-to-BrandConfig Loyalty bridge"
      ],
      "parity_metric": "BusinessConfig and BrandConfig Loyalty fields must remain zero-diff while the one-way trigger and transitional Benefits dual-write remain active; Web old Admin Business route plus Loyalty-field consumers and unregistered direct BusinessConfig Loyalty persistence consumers must remain zero. After expand, LoyaltyProgramPolicy versus BrandConfig and BusinessConfig must also report zero diff before each cutover step.",
      "rollback": "Before trigger split, retain synchronized BusinessConfig and BrandConfig copies. After trigger split, rollback only to an application version that no longer requires BusinessConfig-triggered Loyalty propagation. Never rewrite Loyalty ledger or historical order redemption facts.",
      "exit_criteria": "Old Admin Business Loyalty request/response fields are removed, all runtime readers/writers use dedicated LoyaltyProgramPolicy persistence, the BusinessConfig trigger contains no Loyalty fields, duplicated BrandConfig/BusinessConfig Loyalty columns are dropped in an authorized contraction migration, parity is proven, and active compatibility annotations are removed",
      "remove_pr": "Admin Business contract contraction and Phase A persistence expand/backfill are implemented; remaining work is Phase B triple-write/shadow-read, dedicated read cutover, trigger split, dual-write removal, and final column contraction",
      "deadline": "Before Phase 2 Identity/Customer/Benefits exit",
      "evidence": [
        "apps/api/src/loyalty/public-api.ts",
        "apps/api/src/loyalty/loyalty-policy.contract.ts",
        "apps/api/src/loyalty/loyalty-policy-prisma.writer.ts",
        "apps/api/src/admin/benefits/admin-loyalty-policy.controller.ts",
        "apps/api/src/admin/business/admin-business.controller.ts",
        "apps/api/src/admin/business/admin-business.service.ts",
        "apps/api/src/pos/pos-summary.controller.ts",
        "apps/web/src/lib/api/loyalty.ts",
        "apps/web/src/app/[locale]/(device)/store/pos/payment/page.tsx",
        "apps/api/src/orders/orders.service.ts",
        "apps/api/src/orders/orders-loyalty-redemption.ts",
        "apps/api/src/orders/orders-loyalty-redemption.characterization.spec.ts",
        "apps/api/prisma/schema.prisma",
        "apps/api/prisma/migrations/20260819233000_add_store_config_foundation/migration.sql",
        "apps/api/prisma/migrations/20260831133000_add_loyalty_program_policy/migration.sql",
        "docs/architecture/benefits-loyalty-policy-contraction-plan.md",
        "tools/architecture/context-baseline.json",
        "tools/architecture/scan-architecture.mjs"
      ]
    },
    {
      "compat_id": "brand-store.default-store-identity.v1",
      "status": "active",
      "old_owner": "Implicit literal default store identity in Uber channel tables and single-store helpers",
      "new_owner": "Explicit Store.storeStableId at public boundaries with repository-local DB UUID conversion",
      "dual_write_or_read": [
        "Uber channel configuration rows still default storeId to the literal default identity",
        "Existing rows and callers may omit an explicit store identity"
      ],
      "parity_metric": "Count of rows using the default literal and count of requests resolved through implicit-store fallback; both must reach zero",
      "rollback": "Retain the old nullable/default columns during expand-backfill-cutover; do not reinterpret historical IDs",
      "exit_criteria": "All rows are backfilled, callers pass storeStableId, schema defaults and fallback helpers are removed",
      "remove_pr": "Phase 2 store identity contraction PR",
      "deadline": "Before Phase 2 Brand/Store exit",
      "evidence": [
        "apps/api/prisma/schema.prisma Uber*Config models",
        "SanQ full-site modularization audit section 3.3 and 6.8"
      ]
    },
    {
      "compat_id": "web.api-envelope-direct-payload.v1",
      "status": "active",
      "old_owner": "Checkout direct API payload readers and page-local response unwrap helpers",
      "new_owner": "One Web API client using the global code/message/details response envelope",
      "dual_write_or_read": [
        "Canonical apiFetch and serverApiFetch require the global code/message/details envelope",
        "POS session/login claim, login, auth/me, public store-status and connectivity heartbeat calls use the centralized POS session API adapter; their five browser direct-fetch allowances are removed",
        "Legacy browser direct-fetch debt remains only in Checkout (6 calls), which still owns page-local envelope/direct-payload compatibility"
      ],
      "parity_metric": "Architecture scanner browser direct-fetch debt is reduced from 11 to 6 and must reach zero; POS session/login direct fetches must remain zero, and page-local Checkout envelope/direct-payload readers must be removed",
      "rollback": "Keep only the remaining Checkout legacy calls isolated by architecture allowances until its dedicated risk-scoped migration; POS session/login must stay on the canonical client and must not regain direct-fetch allowances",
      "exit_criteria": "All regular Web JSON API calls use the canonical client, remaining legacy browser direct fetches are zero, and duplicate/page-local ApiEnvelope readers are removed",
      "remove_pr": "POS session/login browser transport migration followed by the dedicated Checkout browser transport migration",
      "deadline": "Before Phase 1 low-risk cleanup exit",
      "evidence": [
        "SanQ full-site modularization audit section 3.4",
        "apps/web/src/lib/api/pos-session.ts",
        "apps/web/src/app/[locale]/(device)/store/pos/PosSessionKeepAlive.tsx",
        "apps/web/src/app/[locale]/(device)/store/pos/login/page.tsx",
        "apps/api/src/pos/pos-devices.controller.ts",
        "tools/architecture/context-baseline.json"
      ]
    },
    {
      "compat_id": "payments.pos-card-legacy.v1",
      "status": "frozen",
      "old_owner": "POS CARD /pos/orders direct paid-order creation",
      "new_owner": "Unified Payment Core plus Clover Terminal followed by idempotent order finalization",
      "dual_write_or_read": [
        "Feature flag false routes to legacy CARD",
        "Feature flag true is reserved for the new terminal flow; the same operation must never call both"
      ],
      "parity_metric": "Terminal payment/provider truth, amount, surcharge, final order binding and refund outcome parity across one complete settlement cycle",
      "rollback": "Set the rollout flag false to route new POS operations back to legacy CARD without rewriting payment history",
      "exit_criteria": "Clover account blocker resolved, real-device acceptance complete, one settlement cycle reconciled, legacy invocation metric is zero",
      "remove_pr": "Payments/Clover Phase G legacy POS contraction PR",
      "deadline": "Before Phase 5B Payments/Clover exit; clock resumes when Clover sandbox identity blocker is resolved",
      "freeze_reason": "Clover Developer sandbox test merchant does not recognize the developer identity; awaiting Clover support",
      "evidence": [
        "docs/payments/clover-pos-integration-charter.md sections 9 and 11"
      ]
    },
    {
      "compat_id": "payments.web-checkout-v1.v1",
      "status": "frozen",
      "old_owner": "CheckoutIntent, CloverPayController/CloverService and Clover Ecommerce /v1 charge path",
      "new_owner": "Channel-neutral Unified Payment Core using canonical Clover Platform v3 truth",
      "dual_write_or_read": [
        "Legacy Web Ecommerce remains the production path until Phase G",
        "Unified core exists but Web traffic must not be switched early"
      ],
      "parity_metric": "Payment ID, amount, surcharge, refund, settlement and old-path invocation metrics across one complete settlement cycle",
      "rollback": "Keep legacy Web routing available until the new Web consumer is accepted; traffic rollback must not rewrite historical facts",
      "exit_criteria": "Uber/Clover external blockers no longer constrain validation, Web cutover is accepted, one settlement cycle matches, old calls are zero",
      "remove_pr": "Payments/Clover Phase G legacy Web contraction PR",
      "deadline": "Before Phase 5B Payments/Clover exit; clock resumes when external verification blockers are resolved",
      "freeze_reason": "Clover sandbox identity verification is blocked; current Web production behavior is explicitly frozen",
      "evidence": [
        "docs/payments/clover-pos-integration-charter.md sections 3.8, 9.1 and 12.5"
      ]
    }
  ],
  "closed": [
    {
      "compat_id": "orders.order-item-components.v1",
      "status": "closed",
      "old_owner": "OrderItem.componentsJson historical fixed-combo reconstruction path",
      "new_owner": "Order-owned immutable item component snapshot",
      "dual_write_or_read": "Removed after production dry-run and contraction",
      "parity_metric": "Production dry-run returned zero actionable rows",
      "rollback": "Git history and the completed audit record; no live fallback retained",
      "exit_criteria": "Backfill planner/CLI and legacy read reconstruction removed",
      "remove_pr": "#2004",
      "deadline": "Completed 2026-08-30",
      "evidence": [
        "docs/orders/order-item-components-backfill.md",
        "commit b8413cd7"
      ]
    }
  ],
  "reviewCandidates": [
    {
      "candidate": "EventEmitter aliases alongside durable outbox events",
      "decisionNeeded": "Measure consumers and duplicate side effects before assigning a compat_id"
    }
  ]
}
