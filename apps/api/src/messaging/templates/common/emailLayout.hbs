<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{brandName}}</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        background: #f5f5f5;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
          'Helvetica Neue', Arial, sans-serif;
      }
      .container {
        max-width: 560px;
        margin: 24px auto;
        background: #ffffff;
        border-radius: 12px;
        padding: 24px;
        color: #222222;
      }
      .brand {
        font-size: 20px;
        font-weight: 600;
        margin-bottom: 16px;
      }
      .footer {
        margin-top: 24px;
        font-size: 12px;
        color: #6b7280;
      }
      a {
        color: #2563eb;
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="brand">{{brandName}}</div>
      {{{body}}}
      <div class="footer">
        <div>Support: <a href="mailto:{{supportEmail}}">{{supportEmail}}</a></div>
        <div>Website: <a href="{{siteUrl}}">{{siteUrl}}</a></div>
        <div>{{storeAddressLine}}</div>
      </div>
    </div>
  </body>
</html>
